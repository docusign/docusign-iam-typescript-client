/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Document status. Last 3 are terminal statuses. Matches enum with similar name in the backend.
 */
export const DocStatus = {
  NotStarted: "NOT_STARTED",
  InProgress: "IN_PROGRESS",
  Canceled: "CANCELED",
  Succeeded: "SUCCEEDED",
  Failed: "FAILED",
} as const;
/**
 * Document status. Last 3 are terminal statuses. Matches enum with similar name in the backend.
 */
export type DocStatus = ClosedEnum<typeof DocStatus>;

/** @internal */
export const DocStatus$inboundSchema: z.ZodNativeEnum<typeof DocStatus> = z
  .nativeEnum(DocStatus);
