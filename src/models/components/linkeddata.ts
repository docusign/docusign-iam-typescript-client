/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Identifies records in external systems which correlate with this agreement.
 */
export type LinkedData = {
  applicationName?: string | undefined;
  objectName?: string | undefined;
  recordId?: string | undefined;
};

/** @internal */
export const LinkedData$inboundSchema: z.ZodType<
  LinkedData,
  z.ZodTypeDef,
  unknown
> = z.object({
  application_name: z.string().optional(),
  object_name: z.string().optional(),
  record_id: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "application_name": "applicationName",
    "object_name": "objectName",
    "record_id": "recordId",
  });
});

export function linkedDataFromJSON(
  jsonString: string,
): SafeParseResult<LinkedData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LinkedData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LinkedData' from JSON`,
  );
}
